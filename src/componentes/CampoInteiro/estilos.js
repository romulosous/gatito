import { StyleSheet } from 'react-native';
import { cores } from '../../estilos';

export default StyleSheet.create({
  campo: {
    height: 31,
    paddingTop: 5,
    borderWidth: 2,
    borderColor: '#A050BE',
    textAlign: 'center'
  }
});